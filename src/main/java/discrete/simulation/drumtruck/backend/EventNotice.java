package discrete.simulation.drumtruck.backend;

public enum EventNotice {
    ALQ, // Arrival of a loaded truck at the queue
    EL, // Exit of a loaded truck from the queue
    EW, // Exit of an empty truck from the weighing queue

}
